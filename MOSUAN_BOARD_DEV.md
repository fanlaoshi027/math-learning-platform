# 墨算（Mosuan Board）开发方向总纲

本分支用于墨算电子书写板/数字白板的持续开发。项目继续采用 **原生 Swift / SwiftUI + Metal**，不改成 Xournal++，Xournal++ 仅作为部分技术思路参考。

## 1. 产品定位

墨算不是“数学专用软件”，而是一块通用的数字书写板：

> **墨算——一块好用的数字书写板。**

主要场景：

- 数字手写笔记
- PDF 批注
- 学习与备课
- 课堂教学
- 网课录制
- 会议/演示
- 思维导图与自由绘图

其中“教师教学”和“数学”是第一阶段的重点深耕场景，但底层产品不能被数学场景锁死。

核心优先级：

```text
书写体验
  > 板书效率
  > 课堂演示
  > 录课能力
  > 扩展课件能力
```

## 2. 核心技术原则

### 2.1 书写优先

任何新增数学工具都不能明显破坏自由书写的低延迟、连续性和自然手感。

重点：

- 低延迟
- 连续采样
- 压感
- 轻量平滑
- 自然起笔/收笔
- 不过度修正

### 2.2 原生结构化对象

自由书写使用 Stroke；几何图形和数学图形使用 `GraphicObject`。

不要把结构化图形保存成截图，也不要把一个逻辑对象拆成多个无关 Stroke/Line。

当前对象体系包括：

- FreehandStroke
- Line
- Arrow
- Polygon
- Rectangle
- Ellipse
- CoordinateSystem
- FunctionGraph
- Group

### 2.3 渲染与数据分离

```text
输入
 ↓
Pointer / Tablet Event
 ↓
书写与工具模型
 ↓
结构化数据
 ↓
GraphicObjectStore / PageState
 ↓
Metal Rendering
```

临时预览属于 Temporary Layer，不应混入正式页面数据。

## 3. 钢笔体系

正式规划为四类：

1. 圆珠笔
2. 钢笔（核心教学笔）
3. 荧光笔
4. 激光笔

不再增加第五种“智能笔”类型。

### 独立参数

以下参数彼此独立：

- 笔型
- 粗细
- 线型
- 颜色

线型：

- 实线
- 虚线
- 点线
- 点划线

### 钢笔手感

钢笔的全局手感参数独立于笔槽：

- 压力响应
- 压力范围
- 稳定化
- 笔尖手感
- 起笔行为
- 收笔行为
- 最小宽度
- 最大宽度

“收藏笔”保存完整组合：

```text
笔型 + 粗细 + 线型 + 颜色
```

最近使用动态保存；收藏长期保存。

## 4. 快速笔盘

长期目标采用类似 ClassIn 的圆形快速控制盘，但不是复制 UI。

原则：

- 固定工具栏负责精确操作
- 圆形笔盘负责课堂快速操作
- 数位笔侧键作为主要触发方式
- 按住侧键 → 出现圆盘 → 悬停选择 → 松开确认
- 圆盘自动避开书写区域和屏幕边缘
- 默认显示 6 个收藏笔
- 更多收藏放入“+ / 更多收藏”

图标统一优先使用 SF Symbols，不使用 Emoji 作为正式 UI 图标。

## 5. 智能直线

智能直线不是第五种笔，而是一种书写模式/手势。

两种方式并存：

1. 明确选择“直线工具”
2. 自由书写后停顿，由系统判断为直线

初始阈值方向：

- <250ms：普通书写
- 250–600ms：候选直线
- 停顿后轻微移动：调整终点
- 松笔：确认

实际阈值必须通过真实板书测试调整。

## 6. 几何工具

第一批：

- 直线
- 箭头
- 矩形
- 圆/椭圆
- 三角形
- 多边形

核心原则：

> **保留教师意图，而不是强制数学标准化。**

例如手绘三角形不应自动强制成为等边三角形。

### 多边形

多边形采用连续点击顶点：

```text
点击第一个点
 ↓
连续点击顶点
 ↓
实时显示下一条预览边
 ↓
点击首点附近闭合
 ↓
一次生成 PolygonObject
```

三角形本质上就是 3 个顶点的 Polygon。

必须支持：

- 每个顶点独立编辑
- 整体移动
- 缩放
- 旋转
- 镜像
- 线颜色
- 线宽
- 线型
- 后续填充

## 7. 多边形当前实现规范

`PolygonObject` 和 `GraphicObject(kind: .polygon)` 是正式数据模型。

当前已经修正一个重要问题：

**不能把一个多边形保存成 N 个 Line 对象。**

正确结构：

```text
PolygonToolModel
 ↓
PolygonObject / GraphicObject(.polygon)
 ↓
GraphicObjectStore
 ↓
PageState
```

一个多边形对应一个对象。

一次完成一个多边形对应一次 Undo。

绘制中的顶点和预览边属于临时状态，闭合后才提交正式对象。

## 8. 选择与对象编辑

统一使用一个“选择工具”。

基础操作：

- 点击对象：选择整个对象
- Shift 点击：多选
- 框选/套索：选择对象
- 拖动：移动
- Delete：删除
- Cmd+C / Cmd+V：复制粘贴

对象高级操作：

- 缩放
- 旋转
- 自定义旋转中心
- 精确角度
- 水平翻转
- 垂直翻转
- 镜像
- 颜色
- 粗细
- 线型
- 填充
- 属性面板

### 多边形节点编辑

下一阶段重点：

```text
选择 Polygon
 ↓
显示顶点控制点
 ↓
拖动某一个顶点
 ↓
只修改对应 vertex
```

整体移动/缩放/旋转仍然使用对象 Transform，而不是重新采样顶点。

## 9. 橡皮

规划三种模式：

1. 局部橡皮
2. 整笔/整个对象删除
3. 圆形区域删除

结构化对象默认优先整体对象删除，避免破坏几何结构。

圆形/区域删除应一次批量删除，并作为一次 Undo。

## 10. 页面与白板

墨算采用数字笔记本模式：

- 多页面
- 左侧缩略图
- 拖动排序
- 新建
- 复制
- 重命名
- 删除
- 页面背景

背景与页面内容分离。

背景包括：

- 空白
- 横线
- 方格
- 点阵
- 数学网格
- 黑板
- 自定义背景
- PDF

新建页面默认继承上一页背景。

缩略图应缓存，仅在页面 dirty 时重新生成。

## 11. 画布操作

规划：

- Space + 拖动：平移
- 触控板双指：平移
- Pinch：缩放
- Cmd+0：适应页面
- Cmd+1：100%
- 后续增加 Minimap

## 12. 护眼显示

护眼是显示层变换，不修改原始数据。

目标：

- 白色背景 → 约 90% 黑色/深色
- 可选深绿、深蓝、自定义色值
- 彩色内容尽量保持色相和饱和度
- 不采用简单 RGB 全通道反色

适用于：

- PDF
- 手写笔迹
- 数学对象
- 图片
- 白板

## 13. 课堂模式

课堂模式的目标是最大化书写区域。

临时层包括：

- 激光笔
- 聚光/强调
- 临时笔迹
- 工具预览
- 选择框

这些内容默认不保存到正式页面。

切换页面时临时层默认清空。

## 14. 录课方向

录课属于后续阶段，不以牺牲书写性能为代价。

初期不自己实现完整视频编码器，优先：

- 录制模式
- 隐藏侧栏和多余 UI
- 画布优先
- REC 状态
- 麦克风状态
- 摄像头状态
- 激光笔进入录制画面
- 与 OBS 配合完成最终编码

## 15. 打印方向

后续支持：

- 全部页面
- 当前页面
- 自定义页码
- 横向/纵向
- 彩色/黑白
- 单面/双面
- 打印 PDF 原内容
- 打印 PDF + Annotation Layer

## 16. 原生文件格式

原生格式为 `.mosuan`。

必须保存结构化数据：

- 文档
- 页面
- PDF
- 原始笔迹采样
- 压力
- 数学对象
- 样式
- Transform
- 分组
- 页面背景
- 页面状态
- 文档版本

目标是长期可编辑、可迁移、可跨平台恢复。

## 17. 跨平台路线

第一阶段：macOS。

后续：

- Windows
- Android 平板

输入层统一抽象为 `PointerEvent`，避免核心逻辑绑定某一个数位板品牌。

优先兼容：

- Wacom
- HUION / 绘王
- XP-PEN
- GAOMON / 高漫
- VEIKK
- One by Wacom
- 系统标准触控笔

## 18. 当前开发优先级

### P0：书写基础

- 压感
- 低延迟
- 连续采样
- 轻量平滑
- 自然起笔/收笔
- 橡皮
- Undo / Redo

### P1：基础结构化对象

- 直线
- 箭头
- 多边形
- 三角形
- 选择
- 移动
- 缩放
- 旋转

### P2：节点编辑与数学图形

- Polygon 顶点编辑
- 矩形/椭圆
- 圆
- 坐标系
- 函数图像
- 几何构造

### P3：教学增强

- 收藏笔
- 圆形快速笔盘
- 图库
- 课堂模式
- 激光笔
- 护眼模式完善

### P4：输出能力

- PDF
- 打印
- 录制
- OBS 联动
- `.mosuan` 完整格式

## 19. 当前实现状态

截至本阶段：

- 原生 SwiftUI + Metal 架构已经建立
- Pen / Stroke 基础链路已经建立
- Line 结构化对象已经建立
- PolygonObject 已建立
- PolygonToolModel 已建立
- 多边形连续点击和首点闭合已经建立
- Undo/Redo 历史事务机制已经建立
- 当前多边形提交已修正为“一个 Polygon 对象”，不再拆成多个 Line

下一步：

> **完成 Polygon 选择后的顶点控制点与单顶点编辑。**

之后再扩展矩形、椭圆、圆、箭头等结构化图形，并继续完善统一选择系统。
