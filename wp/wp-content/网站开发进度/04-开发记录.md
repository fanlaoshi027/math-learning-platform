# 开发记录 · 第 04 卷

项目：`fanlaoshi027/math-learning-platform`
分支：`main`

## 2026-08-31 · 参考效果图：课程中心前台 UI

### 视觉目标

本轮以用户提供的四屏效果图为直接视觉基准，继续实现第 2 屏“课程中心”。重点不是增加课程业务功能，而是把现有课程数据重新组织成更接近正式在线课堂产品的界面。

### 已完成

1. `page-course-center.php`
   - 课程中心从原来的大 Hero + 顶部分类切换，调整为“左侧课程导航 + 右侧课程库”的结构。
   - 左侧加入“课程中心 / 初中数学课程体系”品牌标题。
   - “知识体系”提供全部课程、专题课程、教辅配套筛选。
   - “学习阶段”提供七年级、八年级、九年级筛选。
   - 右侧显示当前课程集合标题、说明和课程卡片。
   - 保留 `mathcourse_course_directory` 作为课程数据与权限层，不重复实现课程查询。

2. `assets/css/course-center-reference.css`
   - 新增课程中心独立视觉覆盖层。
   - PC：左侧约 148px 导航栏 + 右侧课程库。
   - 课程卡片改为四列，更接近参考图的信息密度。
   - 卡片采用白底、细边框、轻阴影、小圆角。
   - 保留课程封面图片和无图片占位方案。
   - 移动端改为顶部横向筛选条，课程卡片两列；超窄屏自动单列。
   - 不修改课程业务逻辑。

3. `functions.php`
   - 加载 `course-center-reference.css`。
   - 修复了资源加载条件中一次误写的临时变量，当前条件已恢复为明确的 `is_page('course-center')`。

### 参考图第 3、4 屏继续实现

4. `assets/css/reference-ui-v3.css`
   - 新增统一视觉覆盖层，覆盖课程中心、专题详情、学习页面。
   - 专题详情页使用白色内容卡、细边框、轻阴影和紧凑课时列表。
   - 学习页面按参考图采用 PC“左视频 + 右课时目录”布局。
   - 移动端切换为“视频优先 + 目录下置”。
   - 统一深海蓝、蓝色、绿色、橙色的状态体系。
   - 不改视频播放器业务功能，只调整外层布局和视觉。

5. `functions.php`
   - 将 `reference-ui-v3.css` 放在现有参考 UI v2 后加载。
   - 学习页、课程中心继续保留原有 CSS 与业务资源，不做破坏式替换。

6. `class-course-player.php`
   - 修正上一课、下一课、课程目录课时链接的目标入口。
   - 现在统一指向专用 `learning` 页面，而不是重新回到 `course-center`。
   - URL 继续携带 `course_id` + `lesson_id`。
   - 播放权限仍由 `Course_Service` / `Access_Service` 决定，没有放宽 HLS 权限。

### 当前完整前台路径

```text
首页
 ↓
课程中心（第 2 屏）
 ↓
课程详情 / 课时目录（第 3 屏）
 ↓
学习课程（第 4 屏）
 ↓
Video.js + MathCourse Video
 ↓
HLS
```

### 重要约束

- 不修改 Tutor LMS 源码。
- 不绕过 `Course_Service` / `Access_Service`。
- 不修改 HLS 防盗链。
- 不删除后台课程封面管理。
- 不删除播放器倍速、快进、快退。
- 不引入 Tailwind、React、Vue 等大型依赖。
- 不增加错题本、笔记、教师笔记等无关功能。
- 前台继续中文为主，手机/微信 WebView 优先。

### Git

- `46b14724`：`style: rebuild course center layout to match reference`
- `02de19f0`：`style: add reference course center visual layout`
- `ce315729`：`fix: correct course center stylesheet condition`
- `39b224c1`：`style: implement reference UI for course center detail and learning`
- `6290496a`：`style: load reference UI v3 across course screens`
- `026293d6`：`fix: keep player lesson navigation on dedicated learning page`

### 下一步

继续以用户提供的效果图进行像素级视觉收口：

1. 检查专题详情页实际渲染后的间距、标题层级、课时卡片密度。
2. 检查学习页播放器实际控制栏，特别是 10 秒快退、10 秒快进、倍速、全屏和底部常驻进度条。
3. 继续优化手机/微信 WebView 的播放器宽度和目录触控尺寸。
4. 检查首页与课程中心卡片的封面比例是否完全统一。
5. 最后进行 PHP/CSS/JS 静态检查，避免 UI 修改影响播放和进度功能。
