# 开发记录 · 第 04 卷

项目：`fanlaoshi027/math-learning-platform`
当前开发分支：`wpui-v2`

## 2026-09-01 · 课程中心页面视觉收口

### 本次目标

继续从 `wpui-v2` 当前断点推进前台 UI，不重新设计业务层。课程中心必须服务以下产品路径：

```text
首页
 ↓
课程中心（所有人可见全部课程）
 ↓
播放页面（试看/授权判断）

登录
 ↓
学习中心（仅登录学员）
 ↓
只显示授权课程 + 学习进度
 ↓
播放页面
```

学员采用老师创建账号并按课程授权的模式，不采用开放注册模式。

### 开发前检查

已读取：

- `wp/wp-content/网站开发接班提示词.md`
- `wp/wp-content/网站开发备忘录.md`
- `wp/wp-content/网站开发进度/00-项目状态.md`
- `wp/wp-content/网站开发进度/README.md`
- `wp/wp-content/网站开发进度/03-开发记录.md`
- `wpthem/page-courses.php`
- `wpthem/page-learning.php`
- `wpthem/style.css`
- `wpthem/header.php`
- `wpthem/footer.php`

### 本次修改

文件：`wpthem/page-courses.php`

1. 保留现有课程数据 shortcode，不在主题重新实现课程业务逻辑。
2. 课程中心页面使用统一 `mc-course-page` 外壳。
3. Hero 区域继续使用深海蓝渐变 + 橙色点缀，强化课程中心品牌识别。
4. 课程列表区域取消额外的白色大容器包裹，避免课程卡片被套在多层视觉容器中。
5. 保留 `mathcourse_course_center` 为首选 shortcode，并以 `mathcourse_course_directory` 作为兼容 fallback。
6. 增加无 shortcode 时的友好 fallback，而不是输出 PHP/业务错误。
7. 保持手机优先的页面间距和圆角结构。
8. 增加 `declare(strict_types=1)`，保持模板代码明确的类型行为。

### 相关现有能力确认

`page-learning.php` 当前已经通过 `mathcourse_video` 统一播放器入口，主题不直接输出 HLS 地址；课程目录由 MathCourse `Course_Service` 数据提供，权限/视频安全逻辑没有因本轮 UI 修改而放宽。

`style.css` 已有 `mc-course-card`、`mc-course-cover`、`mc-course-progress` 等基础视觉类，后续课程卡片视觉优化应优先复用而不是重复造类。

### Git

- `205c542c3d20bbcebc1fc3b82883b82d02b508da`：`refactor: polish course center page shell`
- `f2e32f66c123f8d96f0bee759fbaaa1811062079`：`docs: sync current wpui-v2 project status`

### 测试情况

- 已通过 GitHub 当前分支读取确认修改文件内容已写入 `wpui-v2`。
- 已同步 `00-项目状态.md`。
- 尚未进行真实浏览器视觉回归测试。
- 尚未进行真实 HLS 播放测试。

### 已知风险

1. 当前课程中心实际课程卡片由 MathCourse shortcode 输出，若其内部 CSS 与主题外壳存在覆盖关系，需要在真实页面继续检查。
2. 当前仓库同时存在历史 `wp/wp-content/themes/math-course-theme` 与当前迁移目录 `wpthem` 的代码痕迹，后续修改必须确认当前测试站实际启用主题，避免改错目录。
3. GitHub 接班文件中的示例路径以 `main` 为基准，但本轮严格在 `wpui-v2` 分支执行，未修改 `main`。

### 下一步

1. 检查课程中心实际卡片视觉与移动端两列布局。
2. 检查游客是否只看到课程信息/试看入口，不显示学习进度。
3. 检查登录授权学员的学习中心是否只显示授权课程。
4. 检查“继续学习”是否准确进入对应课程/课时的学习页。
5. 检查学习页当前课时高亮和完成课时同步。
6. 检查播放器 PC 布局：避免“深色大背景 + 外层白色背景”造成多层框架感；进度条必须位于播放器底部控制区域上方并常驻可见。

## 2026-09-01 · 学习中心访问边界收口

### 本次目标

落实产品硬规则：学习中心不是游客页面；学员账号由老师创建并按课程授权，不采用前台开放注册。

### 开发前检查

本轮重新读取：

- `wp/wp-content/网站开发接班提示词.md`
- `wp/wp-content/网站开发备忘录.md`
- `wp/wp-content/网站开发进度/00-项目状态.md`
- `wp/wp-content/网站开发进度/README.md`
- 本卷 `04-开发记录.md`
- `wpthem/header.php`
- `wpthem/page-learning.php`
- `wpthem/functions.php`

确认 `header.php` 已经按登录状态控制学习中心入口，并且主导航/移动菜单均没有“关于老师”。

### 本次修改

文件：`wpthem/functions.php`

增加 `mathcourse_protect_learning_center()`：

1. 后台请求不处理。
2. 已登录用户不处理。
3. 未登录用户直接访问 `/learning-center/` 时跳转到 WordPress 登录页。
4. 登录成功后返回学习中心。
5. 不增加开放注册逻辑。

### 产品规则确认

```text
游客：
首页 → 课程中心 → 试看/课程页面
导航：首页 / 课程中心 / 学员登录

登录学员：
首页 → 课程中心 / 学习中心 → 已授权课程 → 播放页面
导航：首页 / 课程中心 / 学习中心 / 学员账户
```

课程中心继续对所有人开放；学习中心只允许登录用户进入；学习中心内部仍由 MathCourse/Access_Service 决定具体授权课程，不在主题层重复实现课程授权关系。

### Git

- `f1c06d39b7b8f79e1bbcfd08e6b713cd67c7127f`：`feat: protect learning center for authorized students`
- `7a82c0900768e94511088ad86ea500ec44e6f337`：`docs: sync learning center access rules`

### 测试情况

- 已确认修改目标为 `wpui-v2`。
- 已确认 `header.php` 游客不显示学习中心、登录用户才显示学习中心。
- 已确认本轮未修改 `main`。
- 已完成静态代码级检查；尚未在真实浏览器中验证登录跳转和移动端菜单。

### 已知风险

1. 学习中心页面的实际 WordPress 页面 slug 当前按项目约定为 `learning-center`；真实站点若使用不同 slug，需要以后台实际页面为准调整保护条件。
2. 本轮保护的是未登录访问边界；具体“只显示授权课程”仍必须由 MathCourse `Access_Service` 负责，不能由主题绕过业务层。
3. 尚未完成真实 HLS 与浏览器兼容测试。

### 下一步

1. 检查学习中心实际 shortcode/页面入口和授权课程过滤。
2. 检查继续学习链接是否携带正确 course_id / lesson_id。
3. 检查学习页当前课时高亮及完成状态。
4. 检查 Video.js PC/手机布局和底部常驻进度条。
