document.addEventListener('DOMContentLoaded', function () {
    if (!window.Artplayer) return;
    window.Artplayer.CONTEXTMENU = false;
    var INVERT_FILTER = 'brightness(0.9) invert(1) hue-rotate(180deg)';
    function installInvertStyle() { if (document.getElementById('mathcourse-invert-style')) return; var s=document.createElement('style'); s.id='mathcourse-invert-style'; s.textContent='.mathcourse-artplayer.mathcourse-inverted video{filter:'+INVERT_FILTER+'!important;}'; document.head.appendChild(s); }
    function applyInvert(container,on){var video=container.querySelector('video');container.classList.toggle('mathcourse-inverted',!!on);if(video){if(on)video.style.setProperty('filter',INVERT_FILTER,'important');else video.style.removeProperty('filter');}}
    var hlsLoaderPromise=null;
    function ensureHlsLibrary(){if(window.Hls)return Promise.resolve(window.Hls);if(hlsLoaderPromise)return hlsLoaderPromise;hlsLoaderPromise=new Promise(function(resolve,reject){var existing=document.querySelector('script[data-mathcourse-hls-fallback]');if(existing){existing.addEventListener('load',function(){window.Hls?resolve(window.Hls):reject(new Error('Hls unavailable'));});existing.addEventListener('error',reject);return;}var script=document.createElement('script');script.src='https://unpkg.com/hls.js@1.7.1/dist/hls.min.js';script.async=true;script.setAttribute('data-mathcourse-hls-fallback','1');script.onload=function(){window.Hls?resolve(window.Hls):reject(new Error('Hls fallback CDN failed'));};script.onerror=function(){reject(new Error('Hls fallback CDN failed'));};document.head.appendChild(script);});return hlsLoaderPromise;}
    function updateProgressUI(progress,lessonId){if(!progress)return;document.querySelectorAll('.mc-course-player__progress span').forEach(function(el){el.textContent='学习进度 '+Number(progress.percent||0)+'%';});document.querySelectorAll('.mc-course-player__progress strong').forEach(function(el){el.textContent=Number(progress.completed||0)+' / '+Number(progress.total||0)+' 课时';});document.querySelectorAll('.mc-course-player__progress i').forEach(function(el){el.style.width=Number(progress.percent||0)+'%';});document.querySelectorAll('.mc-course-player__sidebar-head span').forEach(function(el){el.textContent=Number(progress.completed||0)+'/'+Number(progress.total||0);});if(lessonId)document.querySelectorAll('.mc-course-player__item').forEach(function(el){if((el.getAttribute('href')||'').indexOf('lesson_id='+lessonId)!==-1){el.classList.add('is-complete');var check=el.querySelector('.mc-course-player__check');if(check)check.textContent='✓';}});}
    document.querySelectorAll('.mathcourse-artplayer[data-video-url]').forEach(function(container){
        var lessonId=parseInt(container.dataset.lessonId||'0',10),courseId=parseInt(container.dataset.courseId||'0',10),url=container.dataset.videoUrl||'',videoType=(container.dataset.videoType||'').toLowerCase();if(!lessonId||!url||(videoType!=='m3u8'&&videoType!=='mp4'))return;
        installInvertStyle();var storageKey='mathcourse_lesson_'+lessonId+'_time',completionSent=false,completedAt=0,restorePending=true,lastSavedSecond=-1,hls=null,invertEnabled=false,recoveryTimer=null;
        function getSavedTime(){try{var raw=localStorage.getItem(storageKey),time=raw===null?0:parseFloat(raw);return Number.isFinite(time)&&time>0?time:0;}catch(e){return 0;}}
        function clearSavedTime(){try{localStorage.removeItem(storageKey);}catch(e){}}
        function savePosition(player,force){var time=Number(player.currentTime||0),duration=Number(player.duration||0);if(!Number.isFinite(time)||time<=0)return;if(duration>0&&time>=duration-.5)return;var value=Math.floor(time*10)/10;if(!force&&Math.floor(value)===lastSavedSecond)return;lastSavedSecond=Math.floor(value);try{localStorage.setItem(storageKey,String(value));}catch(e){}}
        function restorePosition(player){if(!restorePending)return;var saved=getSavedTime(),duration=Number(player.duration||0);if(!saved){restorePending=false;return;}if(!Number.isFinite(duration)||duration<=0)return;if(saved>=duration-5){clearSavedTime();restorePending=false;return;}try{player.currentTime=Math.min(saved,Math.max(0,duration-1));restorePending=false;}catch(e){}}
        function showCompletionPanel(){var panel=document.querySelector('.mc-course-player__completion');if(!panel)return;panel.hidden=false;panel.classList.add('is-visible');}
        function submitCompletion(player){if(completionSent||completedAt||!window.mathcoursePlayer||!mathcoursePlayer.ajax_url||!mathcoursePlayer.nonce)return;completionSent=true;var form=new FormData();form.append('action','mathcourse_complete_lesson');form.append('nonce',mathcoursePlayer.nonce);form.append('lesson_id',String(lessonId));fetch(mathcoursePlayer.ajax_url,{method:'POST',credentials:'same-origin',body:form,keepalive:true}).then(function(response){if(!response.ok)throw new Error('HTTP '+response.status);return response.json();}).then(function(response){if(!response||!response.success)throw new Error('progress rejected');completedAt=Date.now();clearSavedTime();var data=response.data||{};var cid=Number(data.course_id||courseId||0);updateProgressUI(data.progress||null,lessonId);showCompletionPanel();document.dispatchEvent(new CustomEvent('mathcourse_lesson_complete',{detail:{lessonId:lessonId,courseId:cid,progress:data.progress||null}}));document.dispatchEvent(new CustomEvent('mathcourse_progress_updated',{detail:{lessonId:lessonId,courseId:cid,progress:data.progress||null}}));}).catch(function(error){completionSent=false;console.warn('MathCourse: unable to save lesson completion.',error);});}
        function maybeCompleteAtEnd(player){if(completionSent||completedAt)return;var time=Number(player.currentTime||0),duration=Number(player.duration||0);if(Number.isFinite(time)&&Number.isFinite(duration)&&duration>0&&time>=duration-Math.max(1,Math.min(5,duration*.01)))submitCompletion(player);}
        var options={container:container,url:url,id:'mathcourse-lesson-'+lessonId,type:videoType,lang:'zh-cn',theme:'#1677ff',volume:.7,muted:false,autoplay:false,autoPlayback:false,playsInline:true,fullscreen:true,fullscreenWeb:true,setting:true,playbackRate:true,flip:false,aspectRatio:false,screenshot:false,pip:false,fastForward:true,autoOrientation:true,mutex:true,moreVideoAttr:{playsInline:true,'webkit-playsinline':true,preload:'auto'}};
        if(videoType==='m3u8'){
            options.customType={m3u8:function(video,sourceUrl){
                if(video.canPlayType&&video.canPlayType('application/vnd.apple.mpegurl')){video.src=sourceUrl;video.load();return;}
                function attachHls(HlsClass){if(!HlsClass||!HlsClass.isSupported())return false;if(hls)hls.destroy();hls=new HlsClass({enableWorker:true,lowLatencyMode:false,startFragPrefetch:true,backBufferLength:10,maxBufferLength:45,maxMaxBufferLength:90,maxBufferHole:.5,capLevelToPlayerSize:true,startLevel:-1,abrEwmaDefaultEstimate:2500000,abrEwmaFastLive:3,abrEwmaSlowLive:9,fragLoadingMaxRetry:4,fragLoadingRetryDelay:500,fragLoadingMaxRetryTimeout:4000,manifestLoadingMaxRetry:4,manifestLoadingRetryDelay:300,manifestLoadingMaxRetryTimeout:3000,levelLoadingMaxRetry:4,levelLoadingRetryDelay:300,levelLoadingMaxRetryTimeout:3000,nudgeMaxRetry:5,highBufferWatchdogPeriod:2,debug:false});
                    hls.on(HlsClass.Events.MANIFEST_PARSED,function(){if(recoveryTimer){clearTimeout(recoveryTimer);recoveryTimer=null;}});
                    hls.on(HlsClass.Events.ERROR,function(event,data){if(!data||!data.fatal)return;if(data.type===HlsClass.ErrorTypes.NETWORK_ERROR){if(recoveryTimer)return;recoveryTimer=setTimeout(function(){recoveryTimer=null;if(hls){try{hls.startLoad(-1);}catch(e){}}},300);}else if(data.type===HlsClass.ErrorTypes.MEDIA_ERROR){try{hls.recoverMediaError();}catch(e){}}else{try{hls.destroy();}catch(e){}hls=null;}});
                    hls.loadSource(sourceUrl);hls.attachMedia(video);return true;
                }
                if(window.Hls&&attachHls(window.Hls))return;ensureHlsLibrary().then(function(HlsClass){if(!attachHls(HlsClass))console.warn('MathCourse: current browser does not support HLS playback.');}).catch(function(error){console.warn('MathCourse: hls.js failed to load.',error);});
            }};
        }
        var art=new Artplayer(options);window.mathcourseArtPlayers=window.mathcourseArtPlayers||{};window.mathcourseArtPlayers[lessonId]=art;
        art.on('ready',function(){restorePosition(art);art.controls.add({name:'mathcourse-invert',position:'right',html:'☯ 反色',tooltip:'反色播放',click:function(){invertEnabled=!invertEnabled;applyInvert(container,invertEnabled);}});applyInvert(container,invertEnabled);});
        art.on('fullscreen',function(){if(invertEnabled){setTimeout(function(){applyInvert(container,true);},0);setTimeout(function(){applyInvert(container,true);},120);}});art.on('fullscreenWeb',function(){if(invertEnabled){setTimeout(function(){applyInvert(container,true);},0);setTimeout(function(){applyInvert(container,true);},120);}});art.on('resize',function(){if(invertEnabled)applyInvert(container,true);});art.on('video:loadedmetadata',function(){restorePosition(art);if(invertEnabled)applyInvert(container,true);});art.on('video:durationchange',function(){restorePosition(art);});art.on('video:timeupdate',function(){savePosition(art,false);maybeCompleteAtEnd(art);});art.on('video:pause',function(){savePosition(art,true);});art.on('video:seeking',function(){savePosition(art,true);});art.on('video:seeked',function(){maybeCompleteAtEnd(art);});art.on('video:ended',function(){submitCompletion(art);});window.addEventListener('pagehide',function(){savePosition(art,true);});window.addEventListener('beforeunload',function(){savePosition(art,true);});
    });
});