# `.mosuan` 原生文档格式设计

## 1. 定位

`.mosuan` 是 Mosuan Board 的原生矢量教学文档格式。

目标是保存“可继续编辑的课堂黑板”，而不是导出的图片。

## 2. 必须保存的内容

- 文档元数据
- 页面列表
- 页面尺寸与方向
- 背景与背景纹理
- 原始 PDF 内容或其可定位引用
- Annotation Layer
- 自由笔画原始采样
- 压力
- 时间
- 倾斜
- 数学图形
- 样式
- 变换
- 自定义旋转中心
- 分组关系
- 图库插入对象
- 页面状态
- 文档版本

## 3. 设计方向

格式应采用跨平台、可版本化、可迁移的容器设计。

推荐后续评估：

```text
.mosuan
└── ZIP Container
    ├── manifest.json
    ├── document.json
    ├── pages/
    │   ├── page-001.json
    │   └── page-002.json
    └── resources/
        └── PDF / image resources
```

具体二进制编码方案在实际实现前再确定。

## 4. PDF 关联方式

需要支持两种策略评估：

### A. 引用外部 PDF

优点：文件小。

缺点：移动文件后可能丢失关联。

### B. 内嵌 PDF

优点：`.mosuan` 自包含，适合备份和跨设备。

缺点：文件更大。

默认优先考虑“可长期保存、可跨设备迁移”的方案。

## 5. 版本控制

manifest 必须包含格式版本，例如：

```json
{
  "format": "mosuan",
  "version": 1
}
```

未来格式升级必须提供迁移机制，而不是直接破坏旧文件。

## 6. 向后兼容原则

- 不轻易删除字段。
- 新字段允许旧版本忽略。
- 重大结构变化增加格式版本。
- 读取旧文件时尽可能自动迁移。

## 7. 矢量原则

自由笔画保存采样点。

结构化对象保存几何参数。

例如三角形保存三个顶点，而不是保存一张三角形图片。

函数图像保存表达式与参数，而不是保存截图。

## 8. 输出 PDF

导出 PDF 时应尽可能保持原 PDF 内容和 Annotation Layer 的矢量质量。

不能为了简单而把整页先截图再嵌入 PDF。
